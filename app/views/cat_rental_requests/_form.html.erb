<%= form_with(model: cat_rental_request) do |form| %>
  <% if cat_rental_request.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cat_rental_request.errors.count, "error") %> prohibited this cat_rental_request from being saved:</h2>

      <ul>
        <% cat_rental_request.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

 <div>
    <%= form.label :cat_id, style: "display: block" %>
    <%= form.select :cat_id, Cat.all.collect{|c| [c.name,c.id]}, prompt: 'Select a Cat' %>
  </div>

  <div>
    <%= form.label :start_date, style: "display: block" %>
    <%= form.date_field :start_date %>
  </div>

  <div>
    <%= form.label :end_date, style: "display: block" %>
    <%= form.date_field :end_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
